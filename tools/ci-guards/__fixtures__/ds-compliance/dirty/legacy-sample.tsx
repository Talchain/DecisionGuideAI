// FIXTURE — DIRTY (legacy colour tokens). Deliberately violates DS v5 §3.12.
// Isolated under tools/__fixtures__; never scanned by the production gate.
export const legacyClasses = 'text-ink-900 bg-sand-50 border-sky-500'
