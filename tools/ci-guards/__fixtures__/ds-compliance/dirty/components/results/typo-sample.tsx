// FIXTURE — DIRTY (raw panel typography). Deliberately violates DS v5 §2.4.
// Placed under components/results/ so the panel-typography-scoped class applies
// (scope is evaluated on the path relative to the scan root).
export const typoClasses = 'text-sm font-bold'
