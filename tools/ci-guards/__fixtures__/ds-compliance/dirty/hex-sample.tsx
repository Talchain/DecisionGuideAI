// FIXTURE — DIRTY (raw hex colour). Deliberately violates DS v5 §3.12.
// Note: a var(--token, #fallback) form is intentionally NOT used here, because
// the guard correctly excludes fallback hex (scoping decision #1).
export const rawColour = '#AB12CD'
