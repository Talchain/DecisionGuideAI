# Canvas GRAPH — ranked build order

**Derivation only. No product code changed. Nothing executed.**

- **Tree:** fresh blobless clone of `Talchain/DecisionGuideAI` `staging` at `/private/tmp/dgai-build-order-triage-20260918`
- **Head, read not constructed:** `eb7211d7abbc06f2d6ea4cd98c060b8650f25ffc` (`git rev-parse HEAD`), 2026-09-18 15:58:55 +0100, *"panel: the last two unreadable strings (#1679)"*
- **Ran nothing.** No install, no vitest, no playwright, no tsc, no eslint, no browser, no product API call. CI is the only test authority. Specs below are read as **evidence of intended behaviour**, never executed.
- **Inputs triaged:** `GRAPH-BACKLOG.md` (13 deferred) + `reasoning-tab-assessment/ASSESSMENT.md` (11 issues).
- **Scope:** node cards, edges, canvas inspector. The Model tab is out of scope and nothing here touches it.

---

## ⚠⚠ READ FIRST — SEVEN OF TODAY'S NINE ARE **QUEUED, NOT MERGED**

The brief said nine PRs *"landed or are queued"*. Measured against this head, **the queued ones are not in
this tree**, so a naive bytes check reports them as open. They are not open — they are **reviewed work
waiting on a merge**, and a seat dispatched against one is a duplicate.

| Backlog item | State at `eb7211d7` | Verdict |
|---|---|---|
| Edge confidence | **MERGED** `b5a996b0` (#1677) | closed |
| Risk card size | **MERGED** `0318d75a` (#1645) | closed — see NOT-OPEN 4 |
| Rank badge word | **QUEUED #1688** | do not dispatch |
| Influence caption | **QUEUED #1689** | do not dispatch |
| Absences are not one thing | **QUEUED #1690** | do not dispatch |
| Edge dash / unassessed | **QUEUED #1695** | do not dispatch |
| Factor firmness | **QUEUED #1693** | do not dispatch |
| Provenance two marks | **QUEUED #1683** | do not dispatch |

---

## TIER A — stops the product stating something FALSE

| # | Claim | Still open? (verified at file:line) | User-visible harm | Effort | Dependency |
|---|---|---|---|---|---|
| **A1** | **The edge inspector paints an uncertainty nobody stated.** ⭐ NEW — in neither source document. | **OPEN.** `src/canvas/ui/inspector-v2/panels/EdgePanel.tsx:264` — `const strengthStd = edge?.data?.strengthStd ?? 0.15`, **ungated**, no provenance stamp. It feeds `<UncertaintyBand strength={localStrength} std={localStd} />` (`:675`), `SignedStrengthSlider std=` (`:676`) and `<ExpertAnnotation … suffix="σ =" value={localStd}>` (`:768`). `src/canvas/domain/edges.ts:571` — `USER_EDGE_DEFAULTS.strengthStd: 0.15`, unstamped, on every hand-drawn edge. The **canvas line for the same edge refuses this**: `src/canvas/edges/StyledEdge.tsx:1230` routes through `resolveEdgeValueDisplay(…, 'strengthStd')`, and its own comment (`:1221-1223`) names the exact hazard — *"a raw read would paint a ribbon on every hand-drawn edge announcing an uncertainty nobody stated."* | Two surfaces, one edge, opposite verdicts: the board withholds the ribbon, the panel beside it draws a band and prints `σ = 0.15`. A user reads a fabricated confidence interval as their own. | **S** | None. |
| **A2** | **`weight ?? 0.5` and `direction ?? 'positive'` on the same line, also ungated.** Named apart from A1 — same file, **different question** (effect size/sign, not spread). | **OPEN.** `EdgePanel.tsx:260-261`. ⚠ **Narrower than it looks, and this is the honest half:** the *headline* strength readout **is** gated at `:600` (`unset={!resolveEdgeSignedStrengthDisplay(…).show}`). The defaults survive into the **slider position and band geometry**, not the printed word. | A fine-tune control opens pre-positioned at "+0.50 positive" for an edge nobody has assessed — a suggestion dressed as a starting point. | **S** | Ship with A1; same file, same review. |
| **A3** | **Two strength vocabularies, both mounted on the canvas, disagreeing.** (backlog *"ONE strength vocabulary"*, **sharpened and re-scoped**) | **OPEN, but NOT where the backlog said.** ⛔ The backlog's stated instance — canvas chip vs inspector giving one edge two adjectives — **was FIXED** by `6075c3f2` (#1662, 17 Sep); `src/canvas/domain/edgeLabels.ts:9` now imports `getStrengthLabel`, and `:140-148` records the old defect in the past tense. **Do not re-propose that half.** What survives: canvas edge chip → `src/canvas/domain/vocabulary.ts:305-310`, cuts **0.70 / 0.40 / 0.20** → *Very strong / Strong / Moderate / Slight*. Canvas edge **inspector** → `EdgePanel.tsx:802` mounts `EdgeReviewDisagreement`, which imports from `…/model-tab/strengthBands` (`EdgeReviewDisagreement.tsx:37`) → `src/canvas/components/model-tab/strengthBands.ts:19-25`, cuts **0.60 / 0.25 / 0.05** → *Strong / Moderate / Weak / Negligible*. | `|mean| = 0.45` reads **"Strong"** on the line and **"Moderate"** in the panel about it. `0.15` reads **"Slight"** and **"Weak"**. The retired word *"weak"* is live, in the inspector. | **M** | ⛔ **Needs a ruling before code.** `strengthBands.ts:31` `STRENGTH_BAND_MIDPOINTS` are **written back to the model** by the contested quick-set pills, so moving a cut silently re-labels a value a user already chose (`vocabulary.ts:299-302` says the same of its own table: *"change them in the contract first"*). Pick the winning table, do not average them. |

---

## TIER B — helps a team see what they do not know

| # | Claim | Still open? (verified at file:line) | User-visible harm | Effort | Dependency |
|---|---|---|---|---|---|
| **B1** | **Claims are shortened in JS, recover only on hover — and the guard that would notice exempts exactly this class.** (= ASSESSMENT ISSUE 1 + 8, reframed) | **OPEN, and unmeasured.** `src/canvas/nodes/OptionNode.tsx:303` shortens with `compactFactorLabel(fullFactorLabel, NODE_ROW_LABEL_MAX_CHARS)`; recovery is `fullLabel` on **hover** (`:178-192`) plus the popover at `:1479-1487`. `BaseNode.tsx:1636-1647` clamps the title `line-clamp-2` with `title={label}` — again hover. ⭐ **The decisive byte:** `e2e/visual/nodeTextClipping.visual.spec.ts:28-32` — *"It asserts that no text-bearing leaf … overflows its own box horizontally. It does NOT assert anything about strings the product shortens in JAVASCRIPT before rendering."* The founder's 42 measured clips sit **inside the guard's declared blind spot**. | *"Competitive Pressure for Usag…"*, *"Usage-based pricing… Low (0) → Very high (1)"* — the **subject** of the causal claim is the elided part. You cannot argue with a claim you cannot read, and a touch user cannot hover to recover it. | **M** | None to start. ⚠ Do **not** widen the e2e guard to cover JS shortening without first deciding the recovery surface — the guard is right that rendering and content are different questions. |
| **B2** | **The four critical-thinking prompts are 20px, and a mis-hit sends the wrong question to the AI.** (= ASSESSMENT ISSUE 2) | **OPEN.** `src/canvas/nodes/shared/TierInvitation.tsx:59` — `flex flex-col gap-0.5` (**2px**), no slop, no `minHeight`. `TierInvitationButton` → `requestAsk({ text: prompt, … })` (`:83`), so a mis-hit **dispatches a different question**. The blocking invariant is real: `src/canvas/nodes/shared/canvasGlyphScale.ts:103` — *"`NodeQuickActions.targetSize` requires `gap > 2 × slop`"*, and `:201-203` `BOX 20 + SLOP 2 = 24`. ✅ The assessment's own retraction holds — the **quick actions already meet 24px by design**; do not file against them. | The affordances whose entire job is interrogating the model are the only ones below the touch floor. | **M** | ⛔ **Blocked on Paul.** The assessment explicitly asked him whether these are fiddly to hit; the correct fix raises the row gap to ~8px, which makes every factor card taller. **Do not ship unreviewed.** |
| **B3** | **An outcome card cannot say how many options act on it.** | **OPEN.** Target sweep (`rg -a -i` over `src/canvas/nodes/`) for `options act\|optionsTargeting\|inboundOptions\|acting on this` → **0**. **Contrast control, same sweep:** `OutcomeNode.tsx:11` imports `useNodeConnections` and `:161` already computes `inboundConnections` — non-zero, so the probe sees. Today that array drives only a post-analysis *"Depends on:"* list (`:177-196`), unfiltered by node kind and absent pre-analysis. | *"Three of your options move this"* is the one honest, useful thing an outcome card can say today — and it is derivable now. | **S** | None. No producer change. |
| **B4** | **A decision card states its option count only when zoomed out — the moment you can read least.** | **OPEN.** `src/canvas/nodes/DecisionNode.tsx:846-851` — `optionCount` reaches **only** `lodMetric`, the single reduced line. `optionCount` itself is derived correctly at `:333-342` (de-duplicated by target). Its other two uses are a chip *message* (`:467`) and a pre-analysis-gated popover — neither is card-face text at normal zoom. ⚠ **#1690 does NOT close this** — its only `DecisionNode` change is `noOptionsLine: STRUCTURAL_UNSET.noOptions`. | On the anchor card of every model, how many alternatives are in play is hidden at the zoom people actually work at. | **S** | None. |
| **B5** | **No card says *why* it is thin.** | **OPEN.** Sweep for `nothing recorded\|no values set\|little recorded\|model holds` across `src/canvas/nodes/` hits **only `GoalNode.tsx:181-182`**. No factor, risk, outcome or option card face explains its own sparseness. | A board of thin cards reads as *"this tool is empty"* when the truth is *"this model is empty, and here is the next thing to put in it"* — a coaching moment thrown away. | **M** | **Sequence after #1690 merges.** That PR mints `STRUCTURAL_UNSET` and separates *"a decision with no options"* from *"a factor with no number"* — it makes the absence **specific**, which is half of this. Build the *"what to add next"* half on top of its vocabulary, not beside it. |
| **B6** | **The edge stroke merges the two weakest label bands.** | **OPEN.** `src/canvas/utils/graphDisplayCalculations.ts:157` — `EDGE_STROKE_WIDTH_BANDS = { weak: 2, moderate: 3, strong: 4 }`; `:206-211` cuts at **0.7 and 0.4 only**; `:204` `UNSET_EDGE_STROKE_WIDTH = 1`. Three measured rungs against the label's **four** bands, so *Moderate* and *Slight* draw identically. ⚠ #1695 touches this file but changes the **dash** channel, not the widths. | The board's glance-level answer to *"which link matters most"* cannot separate the two weakest bands the words distinguish. | **S** | ⛔ **Depends on A3.** Aligning stroke rungs to a vocabulary that is itself contested bakes the wrong table into a second channel. |

---

## TIER C — backlog, not a seat

| # | Claim | Still open? | Why it waits |
|---|---|---|---|
| **C1** | Selected edge shows `value ± std` and the band-span disclosure. | **OPEN.** Target `±` in `EdgePanel.tsx` → **0**. Contrast: `±` present in 10 other canvas files (`FactorNode.tsx`, `edgeLabels.ts`, `goalTarget.ts`, …) — real absence. | **Partly superseded by A1.** There is no honest `std` to disclose until the fabricated `0.15` is gated. Fix A1, then revisit — the remaining work may be small. |
| **C2** | Nothing arbitrates which surfaces may cover the board at once. (= ISSUES 6 + 7 + 11) | **OPEN.** Target sweep for `overlayArbitr\|surfaceArbitr\|exclusiveSurface\|activeOverlayStore` in `src/canvas` → **0 files**. Contrast: **51** canvas files carry `zIndex`/`z-[` — each choosing its own layer. Real absence, no owner. | **L**, cross-cutting, and needs a precedence ruling before code. ⚠ **ISSUE 7 as written is REFUTED:** `StarterProvenanceBanner.tsx:237-245` has a dismiss button (`aria-label="Dismiss saved-example notice"`, `onClick={() => setDismissed(true)}`, gated at `:182`). It persisted because the driving session never clicked it. The banner is dismissible and its claim is true; the residual complaint is **occlusion**, which is C2, not a toast bug. |
| **C3** | The contrast guard exempts on `aria-hidden`, not on text-recoverability. | **OPEN.** `src/canvas/nodes/__tests__/nodeSystem.semanticColourOnText.spec.ts:318` — `if (/aria-hidden/.test(tag)) { exempted++; continue }`. The design asks for exemption only where the fact is recoverable from **text in the same card**. | Guard-side; **no direct user-visible value**. ⭐ Credit where due: `:325-331` already asserts the exemption count **non-zero** — *"the `aria-hidden` exemption fired for nothing — the tag scan is not discriminating"*. That is the right shape; only the predicate is wrong. |
| **C4** | *"At rest the board is quiet"* — no word or number on an edge until interaction. | **OPEN.** `src/canvas/edges/edgeLabelVisibility.ts:33-42` returns `true` for `isTopStrengthEdge` in **either** view; `:54` `PERSISTENT_LABEL_LIMIT = 3`. | **Lowest, and possibly WITHDRAW.** The code carries a *measured* counter-rationale (`:58-70`: a tried `LEADER_PENALTY` cut worst displacement 252→144 but took label-on-label overlaps 2→3). This item would **remove information** from the board, which cuts against the product's own purpose. It is a design disagreement, not a defect. |

---

## NOT OPEN — do not dispatch a seat. Re-proposing these is worse than proposing nothing.

1. **Influence caption reads as a ranking** — **QUEUED PR #1689.** Verified in the PR diff: adds `influenceRankReadout` / `influenceRankExplanation`, derives `influenceSetSize` off the *same* `ranked` array as `sensitivityRank`, and replaces the bare `{influencePct}%` with `{influenceRank.setSizeText}`. Current bytes still show the bare percentage at `FactorNode.tsx:695,706` — **that is the pre-merge state, not an open item.** ✅ This also closes **ASSESSMENT ISSUE 10** (*"Relative influence — 100%"*).
2. **Rank badge carries a word** — **QUEUED PR #1688.** Diff removes `#{displayMetadata.sensitivityRank}` (current bytes: `BaseNode.tsx:1459`) and ships `sensitivityRankBadgeLabel(`, bound by import to `SENSITIVITY_RANK_LEGEND_NOUN` so badge and legend cannot drift.
3. **Direction is a glyph, not a hue** — **product half DONE at this head.** `StyledEdge.tsx:1637-1677`: the `+`/`−` glyph renders in **Standard view and pre-run**, is gated on `statedDirection` (not the raw defaulted field), uses neutral `typography.edgeLabel text-text-body` — *"Colour was never the load-bearing channel here"* — and carries `aria-label` + `data-edge-id` identity binding. All three suppressions are **non-lossy**: causal lens prints its own signed label (`:1585`), structural edges have no semantic direction, and `strengthRowCarriesDirection` (`:1073-1083`) calls `labelCarriesDirection(…)` to confirm the label *actually states the sign* before suppressing. ⚠ **Residual is a stale comment, not a defect:** `directionStroke.ts:10-11` still reads *"these hues are the primary"* while `:29-32` of the same docblock and `edgePresentation.ts:357-358` both conclude the glyph carries it. CLAUDE.md trap 14 — a false label teaches the next session the wrong thing. **Doc fix, zero user harm.**
4. **Risk card states its size** — **MERGED 17 Sep**, `0318d75a` (#1645). `RiskNode.tsx:41-44` ships `recordedValue` via `nodeRecordedValue`. ⭐ And that commit **measured the producer gap the backlog guessed at**: `RiskNode.tsx:32-39` records `probability` and `impact` present on **0 of 23 risk nodes** across the staging golden path, CEE fixtures and starter captures, while **4 of 23** carry the datum it now renders (`12 months`, `4 months`, two cost figures). So the `{N}% likely` / `{Impact} impact` pair at `:324-327` is **dead by data, not by code**. **This is a CEE/producer item, not a canvas lane** — do not re-file it against the UI.
5. **Risk card states its share of the downside** — **OPEN but BLOCKED.** Target sweep (`downsideShare\|riskContribution\|worst quartile\|contribution to downside`) → **0 files**; contrast: **29** files carry the bare word `downside` — real absence. But item 4 establishes the producer sends **no** risk-level quantities at all, so this needs an ISL/PLoT change first. **Not dispatchable to a canvas seat.**
6. **ISSUE 3 — `uncertainty_drivers` is `"Not provided"` on 80.1% of factors.** Producer-side, Core's. The UI copy (*"Olumi's placeholder — no evidence yet"*) is **honest and must not be suppressed**.
7. **ISSUE 4 — the unexplained tooltip.** Three sightings in captures, **zero reproductions** from a deliberate two-arm test. The assessment's own verdict is UNEXPLAINED, not CONFIRMED. **Not dispatchable.** Its occlusion cost is already carried by C2.
8. **ISSUES 5 and 9 — one console error, one 404.** **UNKNOWN.** Neither can be identified without driving the deployed app, which this run is forbidden to do. Recorded, not ranked. Do not guess a cause.

---

## Scope and instrument notes — what this triage could NOT see

- **Nothing was executed.** Every verdict is a bytes reading of one tree at one SHA. No test result, no runtime behaviour, no deployed-flag posture is claimed anywhere above. Flag-gated mount paths were **not** derived — an item marked OPEN is open *in the source*; whether the deployed flag posture mounts it is a separate question this run did not ask.
- **Queued-PR verdicts rest on `gh pr diff`**, i.e. on the PR head at the time of reading, not on a merge. If a PR is force-pushed or closed, re-derive before trusting rows 1 and 2 of NOT-OPEN.
- ⚠ **One instrument failure, caught and disclosed.** My first sweep for the clipping guard ran `find src -name 'nodeTextClipping*'` and returned **zero** — I nearly recorded *"the guard does not exist"*. The file is at `e2e/visual/nodeTextClipping.visual.spec.ts`; **my search was scoped to `src/` and the artefact was not in `src/`.** It was caught only by re-running with a contrast control (`*.visual.spec.*` → 15 files, so the probe could see). **CLAUDE.md trap 20 / 13e: an absence claim must name the artefact searched, and `src/` is not the repo.** Every absence claim above therefore carries its contrast control inline.
- **`CANVAS-VISUAL-HARNESS-BLIND`** (cited by the assessment as blocking B2) returned **0 hits with no contrast control run**. That is a **blind probe, not an absence** — I make no claim about it either way.
