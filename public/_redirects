# BFF proxies (must come before SPA catch-all)
# Note: /bff/engine/* and /engine/* routing handled by netlify.toml (environment-aware)
/bff/isl/*     https://isl-staging.onrender.com/:splat        200!

# Static routes
/poc        /poc.html                                       200
/fixtures/* /fixtures/:splat                                200

# SPA catch-all (must be last)
/*          /index.html                                     200

# Note: /bff/cee/* is handled by Edge Function (netlify/edge-functions/cee-proxy.ts)
