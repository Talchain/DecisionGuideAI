# Scenario Sandbox PoC Environment Configuration
# Copy to .env.poc and modify as needed for local development
# Usage: docker compose --profile poc --env-file .env.poc up

# ========================================
# SAFE DEFAULTS - ALL POWERFUL FEATURES OFF
# ========================================

# Core service URLs
VITE_EDGE_GATEWAY_URL=http://localhost:3001
GATEWAY_URL=http://localhost:3001
WARP_SERVICE_URL=http://localhost:4311
JOBS_SERVICE_URL=http://localhost:4500
USAGE_METER_URL=http://localhost:4600

# Frontend feature flags (all OFF by default)
VITE_FEATURE_SSE=0
VITE_FEATURE_JOBS_PROGRESS=0
VITE_FEATURE_RUN_REPORT=0
VITE_USE_REAL_REPORT=0
VITE_FEATURE_CONFIDENCE_CHIPS=0
VITE_FEATURE_TELEMETRY=0
VITE_FEATURE_HINTS=0
VITE_STREAM_BUFFER=1
VITE_FEATURE_PARAMS=0
VITE_FEATURE_MD_PREVIEW=0
VITE_FEATURE_COPY_CODE=0
VITE_SIM_MODE=0
VITE_FAST_CANCEL=0

# Backend security & analysis features (all OFF/safe defaults)
SECRET_HYGIENE_ENABLED=0
ALERT_ONLY=1
OPENAPI_ENFORCE=0
LOG_GUARDRAIL_ENABLED=0
TELEMETRY_ENABLED=0

# Service-specific safe defaults
SIM_MODE=0
FAST_CANCEL=0
PROGRESS_ENABLED=0
METER_ENABLED=0

# Database & external services (mocked/disabled)
DATABASE_URL=sqlite::memory:
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=mock-key-for-poc
OPENAI_API_KEY=mock-api-key-for-poc

# Logging & monitoring
LOG_LEVEL=warn
NODE_ENV=production

# ========================================
# TO ENABLE FEATURES FOR TESTING:
# ========================================
# Uncomment and set to 1 only the features you want to test
# Remember: this is a PoC environment, features should remain OFF by default

# Enable streaming:
# VITE_FEATURE_SSE=1

# Enable job progress:
# VITE_FEATURE_JOBS_PROGRESS=1

# Enable run reports:
# VITE_FEATURE_RUN_REPORT=1
# VITE_USE_REAL_REPORT=1

# Enable other UI features:
# VITE_FEATURE_CONFIDENCE_CHIPS=1
# VITE_FEATURE_TELEMETRY=1
# VITE_FEATURE_HINTS=1
# VITE_FEATURE_PARAMS=1
# VITE_FEATURE_MD_PREVIEW=1
# VITE_FEATURE_COPY_CODE=1

# Enable backend security features (use with caution):
# SECRET_HYGIENE_ENABLED=1
# OPENAPI_ENFORCE=1
# LOG_GUARDRAIL_ENABLED=1

# Enable simulation mode for demos:
# VITE_SIM_MODE=1

# Enable fast-cancel (≤150ms):
# VITE_FAST_CANCEL=1

# Enable advanced service features:
# SIM_MODE=1
# FAST_CANCEL=1
# PROGRESS_ENABLED=1
# METER_ENABLED=1