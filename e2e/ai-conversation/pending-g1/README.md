# Pending G1 out-of-lease lines (not for merge)

The full G1 design patch and the end-to-end spec, parked here so they survive the session.
They are to be re-applied on `ai-conversation/card-action-settled-reload` once RC leases `ConversationPanel.tsx:~689`, `guidanceStore.ts:281` and `V5HeldProposalBlock.tsx:~274` (#63 5824726600).
The in-lease half is `d98da259`.
