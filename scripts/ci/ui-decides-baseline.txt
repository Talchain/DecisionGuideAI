# Every place this scanner finds the UI turning a NUMBER into WORDS.
# Lines starting with # are comments; the ratchet strips them, and a test
# pins that the parser drops every comment and keeps every site.
#
# -- ADJUDICATED. Each argued for individually; see
#    src/canvas/__tests__/theUiRendersItDoesNotDecide.spec.ts.
src/canvas/components/DraftLoadingAnimation.tsx:383
src/canvas/components/InsightsPanel.tsx:126
src/canvas/components/ModelQualityScore.tsx:264
#
# NO LONGER NEEDED, and that is the good kind of change: the rule got more
#    precise on 15 Sep (a comparison between two RUNTIME values is a range
#    check, not a chosen cutoff), so these hand-argued exceptions stopped
#    being exceptions:
#      - src/canvas/components/SnapshotManager.tsx:57
#      - src/canvas/components/pre-analysis/ModelSnapshot.tsx:215
#
# -- INHERITED, AND NEWLY VISIBLE ON 15 Sep 2026, when the scanner was
#    widened ONE HOP to the helper modules a render surface imports.
#    NOT ENDORSED. This is the measured starting point, so that no NEW one
#    can be added while these are worked through. Recording an inherited
#    set honestly beats a clean file that cannot see them: a scanner that
#    cannot reach a location returns the same output as one that looked
#    and found nothing.
#
#    FIVE OF THEM WERE ANSWERED BY DELETION RATHER THAN BY RECORDING.
#    `domain/edges.ts` banded an edge coefficient into seven sentences and
#    had NO production consumer (rg -uu: 3 files, all of them itself, its
#    spec, and the comment superseding it; contrast controls NodeChip 22 /
#    BaseNode 105). Deleting it shortened this file, which is the
#    direction it should move in.
#
#    The sharpest of what remains:
#      - src/types/constraints.ts        "Meets all targets" at >= 0.40
#      - src/lib/stability.ts x3         three bands, one carrying ADVICE
#                                        ("Review key inputs")
#      - adapters/plot/v2/responseMapper "shows the strongest expected
#                                        outcome" at winProb > 0.6
src/adapters/cee/client.ts:735
src/adapters/plot/v2/responseMapper.ts:1576
src/canvas/contextMenu/actions.ts:1092
src/canvas/hooks/useModelHealth.ts:148
src/canvas/hooks/useRiskProfile.ts:358
src/canvas/hooks/useRiskProfile.ts:362
src/canvas/hooks/useScienceIcons.ts:145
src/canvas/hooks/useScienceIcons.ts:160
src/canvas/store.ts:7984
src/canvas/stores/readinessStore.ts:782
src/canvas/stores/readinessStore.ts:875
src/canvas/utils/briefValidation.ts:43
src/canvas/utils/ceeAnalysisReadyValidation.ts:159
src/canvas/utils/ceeDataAdapter.ts:328
src/canvas/utils/ceeDataAdapter.ts:331
src/canvas/utils/formatCEEError.ts:84
src/canvas/utils/graphHealthStrings.ts:22
src/canvas/utils/graphHealthStrings.ts:26
src/canvas/utils/limitsStatus.ts:47
src/canvas/utils/limitsStatus.ts:51
src/components/results/TornadoChart.tsx:670
src/components/results/TornadoChart.tsx:687
src/hooks/useCEEDraft.ts:101
src/lib/stability.ts:110
src/lib/stability.ts:123
src/lib/stability.ts:136
src/types/constraints.ts:78
#
# -- INHERITED, AND NEWLY VISIBLE ON 24 Sep 2026 (#1921), for the same reason
#    as the 15 Sep block: `canvas/nodes/OptionNode.tsx` now imports
#    `leaderWithholdCause` from `analysisNewCopy.ts`, which puts that module
#    ONE HOP from a render surface. The site below is byte-identical on
#    staging `24e06704` (blame: 15 Sep) and is not touched by #1921.
#    CLASSIFIED (Canvas, 24 Sep; Panel owns the copy and may overturn):
#    ACCEPTED EXCEPTION. `guaranteedHundredClause` explains the Reasoning
#    tab's OWN bar scale ("the top bar is full width because it is the
#    strongest factor shown, not because it reached 100%"), keyed on the
#    DISPLAYED top figure. It states nothing about the model or the world;
#    its docblock (:2391-2440) records why the figure is not rescaled and
#    why the else-arm names no cause. A self-observation of the display the
#    scanner's `isSelfObservation` cannot see through a function parameter.
#    (`store.ts:7878` -> `7884` above is a pure line shift from #1921's six
#    added lines in `resultsWithholdLeaderClaim`; the line text is identical.)
#    (`store.ts:7884` -> `7887` is a pure line shift from the three lines the
#    `setOutcomeNode` `fromProducerSync` doc comment adds above it (25 Sep
#    2026, the automatic-first-run freshness fix); the line text is identical.)
#    (`store.ts:7887` -> `7894` is a pure line shift from #1974's net +7 lines
#    in `addNodeWithEdge` (the endpoint kinds passed to the structural-link
#    capture); the line text is identical.)
#    (`readinessStore.ts:744` -> `782` and `:840` -> `875` are pure line shifts
#    from the `publishCheckFailure` helper #1982 adds above them (25 Sep 2026,
#    the readiness-outage gate fix); the 429 and 404 branch text is identical.)
#    (`analysisNewCopy.ts:2433` -> `2440`, and the docblock range above by the
#    same +7, is a pure line shift from #1922's seven added lines —
#    `optionFigures.goalOnlyQualifier` and its docblock, above this site — when
#    #1921 and #1922 meet; the line text `: topFigurePercent === 100` is
#    identical. No new site: the scanner's added/gone sets are otherwise empty.)
#    (`contextMenu/actions.ts:912` -> `934` above is a line shift from askAI's
#    step 3 moving from `_sendMessage` to `requestAsk` (24 Sep 2026); the test
#    `attempts < MAX_ATTEMPTS` is identical and is a RETRY BUDGET, not a
#    producer number. Its toast was reworded "Could not send message" ->
#    "Could not open a draft" because nothing is sent any more. The literal is
#    kept INLINE on purpose: hoisting it to a constant would also have made the
#    scanner stop seeing this site, shrinking the baseline by hiding it.)
#    (`analysisNewCopy.ts:2440` -> `2451`, and the docblock range by the same
#    +11, is a pure line shift from `panel/v2-truth-wave2`: one import and the
#    `isSuppressedUnit` guard in `disclosure.tippingPoint`, both above this
#    site. Line text identical; scanner added/gone were exactly this pair.)
#    (`analysisNewCopy.ts:2451` -> `2463`, a pure +12 line shift from Panel
#    bundle #1967: the "latest run did not happen / failed / blocked" status
#    copy (#1957), all above this site. Line text identical
#    (`: topFigurePercent === 100`); CI shard 2 on `ffea022e` reported added
#    and gone as exactly this pair.)
#    (`contextMenu/actions.ts:934` -> `1010` and `store.ts:7887` -> `7895`
#    (`7884` -> `7892` before staging `b017e3c2` moved it +3) are
#    pure line shifts from `canvas/keyboard-delete-guard` (25 Sep 2026): the
#    shared delete entry point, the multi-select edge assessment and their
#    docblocks in `actions.ts`, and corrected `deleteKeyCode` comments in
#    `store.ts`, all above these sites. Line text identical
#    (`if (attempts < MAX_ATTEMPTS) {` / `if (currentTotal + displayBytes >
#    MAX_TOTAL_CHARS) {`); the scanner's added/gone sets were exactly these
#    two pairs.)
#    (`store.ts:7895` -> `7902`, the union of #1991's +8 (landed in U4b) and this
#    branch's +7, both above this site. Line text identical
#    (`if (currentTotal + displayBytes > MAX_TOTAL_CHARS) {`).)
#    (`store.ts:7902` -> `7953`, a pure +51 line shift from
#    `canvas/add-option-hash-chain` (26 Sep 2026): the chained-link capture in
#    `addNodeWithEdge`, the `takePendingStructuralAddEdges` filter and the
#    committed-hash settle, all above this site. Line text identical
#    (`if (currentTotal + displayBytes > MAX_TOTAL_CHARS) {`); added and gone
#    were exactly this pair.)
#    (`store.ts:7953` -> `7968`, a pure +15 line shift from
#    `canvas/add-option-link-contract` (26 Sep 2026): `settleStructuralAdd`
#    also records the committed graph's incident edge keys, above this site.
#    Line text identical; added and gone were exactly this pair.)
#    (`store.ts:7968` -> `7984`, a pure +16 line shift from
#    `canvas/ack-own-add-receipt` (26 Sep 2026, #2070 review): `addNodeWithEdge`
#    records the link it drew in the add intent's `minted` snapshot, above this
#    site. Line text identical; added and gone were exactly this pair.)
#    (`analysisNewCopy.ts:2463` -> `2471`, a pure +8 line shift from Panel
#    bundle 2: copy for the blocker repair ask and #1963's tail subtitles,
#    both above this site. Line text identical; added and gone were
#    exactly this pair in `theUiRendersItDoesNotDecide.spec.ts`.)
#    (`analysisNewCopy.ts:2471` -> `2472`, a pure +1 line shift: the read-back's
#    `decisionRecord.nextActionLabel`, added above this site so the Reasoning
#    tab does not import the record modal. Line text identical.)
#    (`contextMenu/actions.ts:1010` -> `1092`, a pure +82 line shift from
#    `canvas/context-menu-typed-value` (26 Sep 2026): Set value now proposes
#    through the card's writer (`FactorValueWriter`, the range-bound scale gate,
#    the settlement toasts) and Reset was removed, all above this site. Line
#    text identical (`if (attempts < MAX_ATTEMPTS) {`); added and gone were
#    exactly this pair.)
#    (`analysisNewCopy.ts:2472` -> `2489`, a pure +17 line shift from
#    `panel/design-model` (design audit B5): the Reasoning tab's success-row
#    and goal-form copy appended to `successTarget`, above this site. Line text
#    identical (`: topFigurePercent === 100`); the scanner's added and gone
#    were exactly this pair.)
src/components/results/analysisNew/analysisNewCopy.ts:2489
