/**
 * Scenario list page — stub for C.1a.
 *
 * Full implementation (fetch, display, create, delete, navigate) is C.1b Task 4.
 * Wiring points: scenarioService.listScenarios, scenarioService.deleteScenario,
 * useScenario().createScenario.
 */

export default function ScenarioListPage() {
  return (
    <div className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold text-text-header">Scenarios</h1>
      <p className="mt-4 text-text-body">
        Scenario list and management coming in C.1b.
      </p>
    </div>
  )
}
