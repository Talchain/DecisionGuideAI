/**
 * Markdown sanitization utility
 * DEPRECATED: Use sanitize.ts instead
 *
 * Re-exports from central sanitize utility for backwards compatibility
 */

export { sanitizeMarkdown } from './sanitize'
